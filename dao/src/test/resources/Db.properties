database.createGroup=CREATE TABLE mgroup (id INT,name VARCHAR(20), description VARCHAR(20), ownerID INT, picture blob, CREATIONDATE date);
database.createPhones=CREATE TABLE phones (userId INT, phoneNum INT, phoneType varchar(20), id int);
database.cleanGroup=drop table mgroup;
database.cleanPhones=drop table phones;
database.addGroup=insert into mgroup (id, name, description, ownerId) values (1, 'billy group', 'billy fan', 1);
database.createAcc=CREATE TABLE account (id INT, name VARCHAR(20), surname VARCHAR(20) ,age INT, phoneNum INT, \
  address VARCHAR(50), email VARCHAR(50), password VARCHAR(50), photo blob);
database.cleanAcc=drop table account;
database.fill=insert into account (id, name, surname, age, email, password) values (1, 'ivan', 'ivanov', 10, 'list@list.ru', '123');
database.createFriends=create table friends (id int, friendid int);
database.cleanFriends=drop table friends;
